npx express-generator
npm i

npm i dotenv
.env
PORT  = 4000

const dotenv = require('dotenv');
dotenv.config();
process.env-PORT


npm i nodemon
"scripts": {
    // "start": "node ./bin/www"
    "start": "nodemon ./bin/www"

  },
mongodb Altas ma account banaune, network access, database access, db/index ma gayera db connect garne ani export connectDB garne ani app.js ma gayera import connect db garne ani teslai execute garne like connectDB();
